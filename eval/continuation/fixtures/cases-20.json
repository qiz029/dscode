{
  "version": 1,
  "id": "coding-continuation-twenty-v1",
  "cases": [
    {
      "id": "retry-after",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement parseRetryAfter(value, nowMs) in src/retry.mjs. Return a delay in milliseconds. A nonnegative integer Retry-After header is seconds; an HTTP-date header is measured relative to nowMs. Expired dates return 0; invalid or absent headers return null. Never return a negative delay. Do not change the exported function name or visible test file.",
      "latest": "Correction: surrounding whitespace in a numeric header is allowed, and numeric zero must return 0 rather than null. Continue the implementation and verify it.",
      "pressureRatio": 0.53,
      "files": {
        "src/retry.mjs": "export function parseRetryAfter(value, nowMs) {\n  // TODO: finish this parser.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { parseRetryAfter } from './src/retry.mjs';\nassert.equal(parseRetryAfter('2', 0), 2000);\nassert.equal(parseRetryAfter('bad', 0), null);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/retry.mjs"
      ]
    },
    {
      "id": "page-window",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement pageWindow(total, offset, limit) in src/paging.mjs. Inputs are nonnegative integers. Return {start,end,nextOffset} for a half-open window. Clamp start to total and end to total. nextOffset is the end only if the window advanced and more items remain; otherwise it is null. Do not modify the visible test file.",
      "latest": "Correction: limit 0 must produce an empty window and nextOffset null, even if more items remain. Finish and verify the implementation.",
      "pressureRatio": 0.84,
      "files": {
        "src/paging.mjs": "export function pageWindow(total, offset, limit) {\n  // TODO: handle clamping and continuation.\n  return { start: offset, end: offset + limit, nextOffset: null };\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { pageWindow } from './src/paging.mjs';\nassert.deepEqual(pageWindow(10, 2, 3), { start: 2, end: 5, nextOffset: 5 });\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/paging.mjs"
      ]
    },
    {
      "id": "quota-reserve",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement reserve(state, id, amount) in src/quota.mjs. state has a balance and reservations object. Return {ok,state}; never mutate the input state or its reservations. Reject nonpositive or noninteger amounts and insufficient balance with ok false and the original state object. On success decrement balance and add reservations[id]=amount. Repeating an already reserved id was originally specified as idempotent, returning ok true and the original state object.",
      "latest": "Correction to idempotency: the same id with the same amount is still a successful no-op, but the same id with a different amount must fail without changing state. Finish and verify this latest rule.",
      "pressureRatio": 0.86,
      "files": {
        "src/quota.mjs": "export function reserve(state, id, amount) {\n  // TODO: preserve input identity on no-op and failure.\n  return { ok: false, state };\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { reserve } from './src/quota.mjs';\nconst state = { balance: 9, reservations: {} };\nassert.deepEqual(reserve(state, 'job-1', 3), { ok: true, state: { balance: 6, reservations: { 'job-1': 3 } } });\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/quota.mjs"
      ]
    },
    {
      "id": "normalize-slug",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement slugify(value): lowercase an ASCII title, trim it, replace each run of non-ASCII-letter-or-digit characters with one hyphen, and remove edge hyphens. Return an empty string when nothing remains. Do not mutate input. Edit only src/normalize-slug.mjs.",
      "latest": "Correction: repeated separators must collapse to one hyphen, including punctuation next to spaces. Finish and run the visible checks.",
      "pressureRatio": 0.28,
      "files": {
        "src/normalize-slug.mjs": "export function slugify(value) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { slugify } from './src/normalize-slug.mjs';\nfor (const [args, expected] of [[[\"Hello World\"],\"hello-world\"]]) assert.deepStrictEqual(slugify(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/normalize-slug.mjs"
      ]
    },
    {
      "id": "parse-port",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement parsePort(value): accept a string of decimal digits with optional surrounding whitespace, return its integer port when it is in 1..65535, otherwise null. Leading zeros are allowed. Reject signs, decimals and non-string inputs. Edit only src/parse-port.mjs.",
      "latest": "Correction: zero is invalid even if written as 0000; 65535 is valid. Finish and run the visible checks.",
      "pressureRatio": 0.28,
      "files": {
        "src/parse-port.mjs": "export function parsePort(value) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { parsePort } from './src/parse-port.mjs';\nfor (const [args, expected] of [[[\" 8080 \"],8080]]) assert.deepStrictEqual(parsePort(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/parse-port.mjs"
      ]
    },
    {
      "id": "merge-ranges",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement mergeRanges(ranges) for inclusive integer [start,end] pairs. Return a new array sorted by start, merging overlapping ranges. Never mutate the input array or its pairs. Edit only src/merge-ranges.mjs.",
      "latest": "Correction: adjacent inclusive ranges also merge: [1,2] and [3,5] become [1,5]. Finish and run the visible checks.",
      "pressureRatio": 0.28,
      "files": {
        "src/merge-ranges.mjs": "export function mergeRanges(ranges) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { mergeRanges } from './src/merge-ranges.mjs';\nfor (const [args, expected] of [[[[[1,3],[2,5]]],[[1,5]]]]) assert.deepStrictEqual(mergeRanges(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/merge-ranges.mjs"
      ]
    },
    {
      "id": "unique-tags",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement normalizeTags(tags): trim and lowercase each string, drop empty results, deduplicate, and return a new array in first-seen order. Do not change the input array. Edit only src/unique-tags.mjs.",
      "latest": "Correction: deduplication happens after trimming and lowercasing, so \" Foo \" and \"foo\" are the same tag. Finish and run the visible checks.",
      "pressureRatio": 0.28,
      "files": {
        "src/unique-tags.mjs": "export function normalizeTags(tags) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { normalizeTags } from './src/unique-tags.mjs';\nfor (const [args, expected] of [[[[\"A\",\"b\"]],[\"a\",\"b\"]]]) assert.deepStrictEqual(normalizeTags(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/unique-tags.mjs"
      ]
    },
    {
      "id": "choose-backoff",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement backoff(attempt, base, cap) for nonnegative integer inputs: return base multiplied by 2 to the attempt power, capped at cap. Inputs are finite and no mutation is needed. Edit only src/choose-backoff.mjs.",
      "latest": "Correction: the cap also applies at attempt zero; never return a value greater than cap. Finish and run the visible checks.",
      "pressureRatio": 0.28,
      "files": {
        "src/choose-backoff.mjs": "export function backoff(attempt, base, cap) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { backoff } from './src/choose-backoff.mjs';\nfor (const [args, expected] of [[[2,100,1000],400]]) assert.deepStrictEqual(backoff(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/choose-backoff.mjs"
      ]
    },
    {
      "id": "split-batches",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement splitBatches(items, size): split an array into consecutive new arrays of at most positive integer size, keeping order and not mutating items. Edit only src/split-batches.mjs.",
      "latest": "Correction: an empty items array returns [] rather than a single empty batch. Finish and run the visible checks.",
      "pressureRatio": 0.45,
      "files": {
        "src/split-batches.mjs": "export function splitBatches(items, size) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { splitBatches } from './src/split-batches.mjs';\nfor (const [args, expected] of [[[[1,2,3],2],[[1,2],[3]]]]) assert.deepStrictEqual(splitBatches(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/split-batches.mjs"
      ]
    },
    {
      "id": "redact-token",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement redactToken(text): in a string, replace each lowercase token=<non-whitespace-value> occurrence with token=[REDACTED]. Leave other text untouched. Edit only src/redact-token.mjs.",
      "latest": "Correction: redact every occurrence in the string, not just the first one. Finish and run the visible checks.",
      "pressureRatio": 0.45,
      "files": {
        "src/redact-token.mjs": "export function redactToken(text) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { redactToken } from './src/redact-token.mjs';\nfor (const [args, expected] of [[[\"token=abc\"],\"token=[REDACTED]\"]]) assert.deepStrictEqual(redactToken(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/redact-token.mjs"
      ]
    },
    {
      "id": "parse-duration",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement parseDuration(text): parse a trimmed nonnegative integer followed immediately by s, m, or h into milliseconds. Return null for invalid input. Units mean seconds, minutes and hours. Edit only src/parse-duration.mjs.",
      "latest": "Correction: zero is valid for every unit; do not treat 0s as missing. Finish and run the visible checks.",
      "pressureRatio": 0.45,
      "files": {
        "src/parse-duration.mjs": "export function parseDuration(text) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { parseDuration } from './src/parse-duration.mjs';\nfor (const [args, expected] of [[[\"2m\"],120000]]) assert.deepStrictEqual(parseDuration(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/parse-duration.mjs"
      ]
    },
    {
      "id": "sum-ledger",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement netAmount(entries): entries have positive integer amount and status. Sum posted amounts and subtract refunded amounts; ignore pending entries. Do not mutate entries. Edit only src/sum-ledger.mjs.",
      "latest": "Correction: a refunded entry subtracts its amount even when no matching posted entry appears in the same input. Finish and run the visible checks.",
      "pressureRatio": 0.45,
      "files": {
        "src/sum-ledger.mjs": "export function netAmount(entries) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { netAmount } from './src/sum-ledger.mjs';\nfor (const [args, expected] of [[[[{\"amount\":10,\"status\":\"posted\"},{\"amount\":3,\"status\":\"pending\"}]],10]]) assert.deepStrictEqual(netAmount(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/sum-ledger.mjs"
      ]
    },
    {
      "id": "latest-by-key",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement latestByKey(rows): each row has key, integer revision and value. Return an object mapping each key to the value at its highest revision. Do not mutate rows. Edit only src/latest-by-key.mjs.",
      "latest": "Correction: when revisions tie for a key, the later row in input order wins. Finish and run the visible checks.",
      "pressureRatio": 0.45,
      "files": {
        "src/latest-by-key.mjs": "export function latestByKey(rows) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { latestByKey } from './src/latest-by-key.mjs';\nfor (const [args, expected] of [[[[{\"key\":\"a\",\"revision\":1,\"value\":\"old\"},{\"key\":\"a\",\"revision\":2,\"value\":\"new\"}]],{\"a\":\"new\"}]]) assert.deepStrictEqual(latestByKey(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/latest-by-key.mjs"
      ]
    },
    {
      "id": "compare-version",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement compareVersion(a,b) for dotted nonnegative integer components. Compare numerically from left to right and return -1, 0 or 1. Missing components count as zero. Edit only src/compare-version.mjs.",
      "latest": "Correction: trailing zero components do not change equality: 1.2 and 1.2.0 compare equal. Finish and run the visible checks.",
      "pressureRatio": 0.84,
      "files": {
        "src/compare-version.mjs": "export function compareVersion(a, b) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { compareVersion } from './src/compare-version.mjs';\nfor (const [args, expected] of [[[\"1.2\",\"1.10\"],-1]]) assert.deepStrictEqual(compareVersion(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/compare-version.mjs"
      ]
    },
    {
      "id": "mask-email",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement maskEmail(email): for a simple address with one @, nonempty local part and domain, keep the first local character and full domain, replace every later local character with *. Return null for malformed addresses. Edit only src/mask-email.mjs.",
      "latest": "Correction: a one-character local part remains unchanged; do not add a star. Finish and run the visible checks.",
      "pressureRatio": 0.84,
      "files": {
        "src/mask-email.mjs": "export function maskEmail(email) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { maskEmail } from './src/mask-email.mjs';\nfor (const [args, expected] of [[[\"alice@example.com\"],\"a****@example.com\"]]) assert.deepStrictEqual(maskEmail(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/mask-email.mjs"
      ]
    },
    {
      "id": "intersect-interval",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement intersect(a,b) for half-open integer intervals [start,end). Return their intersection as a new two-element array, or null when empty. Do not mutate inputs. Edit only src/intersect-interval.mjs.",
      "latest": "Correction: merely touching endpoints is empty, so [1,3) and [3,5) have no intersection. Finish and run the visible checks.",
      "pressureRatio": 0.84,
      "files": {
        "src/intersect-interval.mjs": "export function intersect(a, b) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { intersect } from './src/intersect-interval.mjs';\nfor (const [args, expected] of [[[[1,5],[3,7]],[3,5]]]) assert.deepStrictEqual(intersect(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/intersect-interval.mjs"
      ]
    },
    {
      "id": "count-by-type",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement countByType(events): return an object counting each nonempty string event.type. Ignore entries with missing or non-string types. Do not mutate events. Edit only src/count-by-type.mjs.",
      "latest": "Correction: an empty-string type is ignored rather than counted as a key. Finish and run the visible checks.",
      "pressureRatio": 0.84,
      "files": {
        "src/count-by-type.mjs": "export function countByType(events) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { countByType } from './src/count-by-type.mjs';\nfor (const [args, expected] of [[[[{\"type\":\"a\"},{\"type\":\"a\"},{\"type\":\"b\"}]],{\"a\":2,\"b\":1}]]) assert.deepStrictEqual(countByType(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/count-by-type.mjs"
      ]
    },
    {
      "id": "merge-config",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement mergeConfig(base, override): return a new shallow-merged object without mutating either input. Override values replace base values, including false and zero. Edit only src/merge-config.mjs.",
      "latest": "Correction: a null override means inherit the base value instead of replacing it; false and zero still replace. Finish and run the visible checks.",
      "pressureRatio": 0.84,
      "files": {
        "src/merge-config.mjs": "export function mergeConfig(base, override) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { mergeConfig } from './src/merge-config.mjs';\nfor (const [args, expected] of [[[{\"a\":1},{\"a\":2}],{\"a\":2}]]) assert.deepStrictEqual(mergeConfig(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/merge-config.mjs"
      ]
    },
    {
      "id": "sort-priority",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement sortByPriority(items): return a new array sorted by descending numeric priority. Do not mutate the original array or its objects. Edit only src/sort-priority.mjs.",
      "latest": "Correction: sorting must be stable for equal priorities; keep their input order. Finish and run the visible checks.",
      "pressureRatio": 0.84,
      "files": {
        "src/sort-priority.mjs": "export function sortByPriority(items) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { sortByPriority } from './src/sort-priority.mjs';\nfor (const [args, expected] of [[[[{\"id\":\"a\",\"priority\":1},{\"id\":\"b\",\"priority\":3}]],[{\"id\":\"b\",\"priority\":3},{\"id\":\"a\",\"priority\":1}]]]) assert.deepStrictEqual(sortByPriority(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/sort-priority.mjs"
      ]
    },
    {
      "id": "decode-query",
      "system": "You are a coding agent finishing a small JavaScript task. Inspect the files with tools, edit only the requested source file, run the visible tests, and finish with a short status. Earlier user requirements remain binding unless the latest user message corrects them.",
      "history": "Implement parseQuery(query): parse a URL query string (without ?) into an object of decoded key/value strings. Decode percent escapes and plus as space. When a key repeats, the last value wins. Edit only src/decode-query.mjs.",
      "latest": "Correction: a key without = has an empty-string value; do not omit it. Finish and run the visible checks.",
      "pressureRatio": 0.84,
      "files": {
        "src/decode-query.mjs": "export function parseQuery(query) {\n  // TODO: complete this task.\n  return null;\n}\n",
        "visible.mjs": "import assert from 'node:assert/strict';\nimport { parseQuery } from './src/decode-query.mjs';\nfor (const [args, expected] of [[[\"a=1&b=2\"],{\"a\":\"1\",\"b\":\"2\"}]]) assert.deepStrictEqual(parseQuery(...structuredClone(args)), expected);\nconsole.log('visible checks passed');\n"
      },
      "writable": [
        "src/decode-query.mjs"
      ]
    }
  ]
}
