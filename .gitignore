node_modules/
.runtime/
.research/
.env
.env.*
!.env.example
config/*.local.yml
artifacts/local/
*.log
.DS_Store

config/*.local.json

# Generated release packages and machine-specific verification evidence
artifacts/

# Packed by `make candidates` for a publish job
release-candidates.tar.gz

# Local npm cache configured in .npmrc for the test runtime and probe tooling
.npm-cache/

# Compiled output of the vendored terminal (built by scripts/build-tui.mjs for release)
packages/tui/lib/
